# TODO 将来在docker中部署服务可以去除补丁。
# RocketMQ Broker 配置
# brokerIP1=sky-broker:
# - Docker 内 (Console): DNS 解析到 Broker 容器 IP，直连
# - 宿主机 (sky-server): hosts 映射 127.0.0.1，通过 Docker 端口映射访问
brokerIP1=sky-broker
